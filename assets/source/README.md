# Asset sources

Store original or editable visual/audio inputs here. Do not place release
derivatives in this directory. Every path-backed source used by a registry entry
must include a matching `original_hash`.
